import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const Settings = () => {
	return (
		<SafeAreaView>
			<Text>Settings</Text>
		</SafeAreaView>
	);
};

export default Settings;
